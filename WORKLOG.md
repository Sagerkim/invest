# 작업 로그

프로젝트: 금투협 증시 수급지표 일일 자동 수집 → 텔레그램 알림 + 웹 대시보드
저장소: <https://github.com/Sagerkim/invest>
로컬: `C:\Users\ASUS\projects\kofia-daily`

---

## 2026-07-30 · 초기 구축 (90분 MVP)

### 배경

직장 다니는 개인 투자자. 장 시작 전에 시장 수급을 확인할 시간이 없다.
**투자자예탁금**(대기 매수자금)과 **신용융자**(빚투 규모)를 매일 아침 자동으로 받아보는 것이 목표.
PC가 꺼져 있어도 동작해야 하므로 클라우드 실행이 전제.

### 결정과 근거

| 결정 | 근거 |
|---|---|
| 실행 위치 = **GitHub Actions** | 출근 후 PC가 꺼져도 동작해야 함. 무료. 로컬에 런타임 설치 불필요 |
| 데이터 출처 = **`/stat/main.do`** | 서버가 값을 HTML에 직접 박아 내려줌. 브라우저·API키 없이 안정적으로 파싱 가능 |
| 대시보드 = **GitHub Pages 정적 페이지** | 별도 서버·비용 없음. 데이터 JSON을 저장소에 커밋하면 그대로 서빙 |
| 증감은 **직접 계산** | 출처 페이지의 증감란이 비어 있는 경우가 있어(신용융자 등) 신뢰 불가 |
| 지표 3종 | 요청받은 예탁금·신용융자 + 같은 화면에서 공짜로 얻는 CMA잔고 |

### 막혔던 것 — FreeSIS 내부 API 역공학 (실패, 기록용)

시계열 데이터를 얻으려 `증시자금추이` 화면의 내부 API를 뚫으려 시도했으나 실패.
**같은 삽질을 반복하지 않기 위해 알아낸 사실을 남긴다.**

- 살아있는 엔드포인트는 `POST https://freesis.kofia.or.kr/meta/getMetaDataList.do` **하나뿐**
  (`/stat/getStatList.do`, `/menu/getMenuList.do` 등은 전부 에러 HTML 반환)
- 응답 형태: `{"unit":"","ds1":[...],"dsmHeader":""}`, 컬럼명은 `TMPV1`~`TMPV6` 같은 익명 형태
- `OBJ_NM`은 **17자리 화면ID + 2자리 접미사 = 19자** 고정.
  19자가 아니면 서버가 예외를 던져 에러 페이지를 반환한다(길이 검증에 이용 가능)
- 화면ID: 증시자금추이 `STATSCU0100000060`, 신용공여 `STATSCU0100000070`, 대차거래 `STATSCU0100000140`
- **시도한 것 (전부 빈 결과)**
  - 접미사 26×26 전수(AA~ZZ)를 060/070에 대해 탐색 → 0건
  - `STATSCU01` 하위 ID 10~2500 스윕 → 090/140/150 3건만 응답(모두 정적 헤더 행, 값은 null)
  - 카테고리 코드 01~25 × ID 10~300 스윕 → 추가 수확 없음
  - 접두어 18종(STATSST/STATSBD/STATSFD 등) → 전부 0건
  - 날짜 파라미터 후보 32개 동시 투입(shotgun) → 무효
  - 세션 쿠키(WMONID) + Referer + XHR 헤더 부여 → 무효
  - 화면 정의 파일(`/stat/app/**/*.clx.js`) 직접 요청 → 서버가 차단(에러 페이지)
- **결론**: 파라미터 이름은 화면 정의에 종속적이라 추측으로는 못 맞춘다.
  **브라우저 개발자도구에서 실제 요청 1건을 캡처하는 것이 유일하게 확실한 경로.**

### 결과

- 수집기 로컬·클라우드 양쪽에서 실제 값 검증 완료
  (투자자예탁금 109.593조 / 신용융자 32.995조 / CMA 104.396조, 2026-07-29 기준)
- GitHub Actions 실행 성공 → 데이터 수집·커밋까지 확인 (커밋 `99cffec`)
- 대시보드: 1년 기본 기간, 기간 버튼(1·3·6개월/1년/전체), 날짜 지정 조회, 일별 표, 추이 그래프

### 환경 변경

- 로컬 검증을 위해 **Python 3.12.10 설치**(winget, 사용자 영역).
  원래 계획엔 없었으나 코드 검증 없이 데모하는 위험이 커서 추가.

---

## 현재 상태

| 항목 | 상태 |
|---|---|
| 코드 GitHub 반영 | ✅ |
| Actions 수집·커밋 | ✅ 동작 확인 |
| 텔레그램 발송 | ⛔ Secrets 미등록으로 실패 |
| GitHub Pages | ⛔ 미활성화(404) |
| 과거 데이터 | ⚠️ 1일치. 매일 실행분이 누적되는 구조 |

### 남은 설정 (저장소 소유자만 가능)

1. Secrets: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`(=`7957619237`)
2. Variables: `DASHBOARD_URL` = `https://sagerkim.github.io/invest/`
3. Pages: Settings → Pages → `main` / `/docs`

---

## 백로그 (우선순위 순)

1. **과거 1년치 백필** — `증시자금추이` 화면의 요청을 개발자도구로 캡처해
   `fetch_kofia.py`에 별도 함수로 추가. 이게 되면 그래프가 즉시 1년치로 채워진다.
2. **수집 실패 알림** — 워크플로 실패 시 텔레그램으로 통보(현재는 조용히 실패)
3. **임계값 경보** — 예: 신용융자 35조 돌파, 예탁금 전일 대비 ±3조 등
4. **지표 확대** — 코스피/코스닥 지수, 국고채·회사채 금리(이미 같은 페이지에 있음, 파싱만 추가)
5. **주말·공휴일 처리** — 값이 갱신되지 않은 날은 알림 생략
6. **데이터 정합성 점검** — 전일 대비 이상치(예: 10% 이상 급변) 발생 시 로그 경고

---

## 운영 런북

### 정상 동작 확인

- Actions 탭에서 마지막 실행이 ✅ 인지
- `docs/data/history.json` 의 마지막 날짜가 최근 영업일인지
- 텔레그램에 아침 메시지가 왔는지

### 자주 나는 문제

| 증상 | 원인 | 조치 |
|---|---|---|
| `필수 지표를 찾지 못했습니다` | 출처 페이지 구조 변경 | `DEBUG=1`로 실행해 파싱 결과 확인 → `fetch_kofia.py`의 `INDICATORS`/정규식 수정 |
| 텔레그램만 실패 | 토큰 만료 또는 봇 차단 | BotFather `/token`으로 재발급 후 Secret 갱신 |
| 알림이 안 옴 | 봇에게 `/start`를 한 적 없음 | <https://t.me/Sage_invest_bot> 에서 시작 |
| 대시보드가 빈 화면 | `history.json` 미커밋 | Actions 로그의 커밋 단계 확인 |
| cron이 늦게 돎 | 무료 러너 혼잡 | 정상. 수 분~수십 분 지연될 수 있음 |

### 값 검증

의심스러우면 <https://freesis.kofia.or.kr/stat/main.do> 를 직접 열어
'투자자예탁금', '신용융자' 숫자와 `docs/data/latest.json` 을 대조한다.
단위 주의: 페이지는 **백만원**, 우리 데이터는 **조원**(÷1,000,000).
